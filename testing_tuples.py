#tuples - they are not changebles insteaof [] we use ()
numbers = (1,2,5,2,7)
print(numbers[1])
# to know how often in tuple a number 2 is present:
print(numbers.count(2))
#to know which index has a number:
print(numbers.index(2))
